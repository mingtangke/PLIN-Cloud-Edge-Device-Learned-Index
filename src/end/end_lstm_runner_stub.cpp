#include <iostream>
#include <string>

void end_lstm_runner_init(const std::string& model_path) {
    std::cout << "[end_lstm_runner] stub (no libtorch); model_path="
              << model_path << std::endl;
}
